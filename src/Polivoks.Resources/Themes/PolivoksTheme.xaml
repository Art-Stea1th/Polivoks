<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Color x:Key="ChassisColor">#FF070B10</Color>
    <Color x:Key="ModuleFaceColor">#FF1A2A2C</Color>
    <Color x:Key="ModuleHeaderColor">#FF0D171C</Color>
    <Color x:Key="AccentGold">#FF4FD7D6</Color>
    <Color x:Key="KeyWhite">#FFD8E1DE</Color>
    <Color x:Key="KeyBlack">#FF090B0F</Color>

    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource ChassisColor}"/>
    <SolidColorBrush x:Key="ModuleFaceBrush" Color="{StaticResource ModuleFaceColor}"/>
    <SolidColorBrush x:Key="ModuleHeaderBrush" Color="{StaticResource ModuleHeaderColor}"/>
    <SolidColorBrush x:Key="SectionBackgroundBrush" Color="{StaticResource ModuleFaceColor}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentGold}"/>
    <SolidColorBrush x:Key="TextBrush" Color="#FFE4F3F4"/>
    <SolidColorBrush x:Key="KeyWhiteBrush" Color="{StaticResource KeyWhite}"/>
    <SolidColorBrush x:Key="KeyBlackBrush" Color="{StaticResource KeyBlack}"/>

    <Style TargetType="Menu">
        <Setter Property="Background" Value="#FF070708"/>
        <Setter Property="Foreground" Value="#FFBFC4C8"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="FontSize" Value="11"/>
    </Style>

    <ControlTemplate x:Key="TopLevelMenuItemTemplate" TargetType="{x:Type MenuItem}">
        <Grid SnapsToDevicePixels="True">
            <Border x:Name="ItemSurface"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    BorderThickness="0"
                    Padding="8,4">
                <ContentPresenter ContentSource="Header"
                                  RecognizesAccessKey="True"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  TextElement.Foreground="#FFBFC4C8"/>
            </Border>
            <Popup x:Name="PART_Popup"
                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                   Placement="Bottom"
                   AllowsTransparency="True"
                   Focusable="False"
                   PopupAnimation="None">
                <Border Background="#FF0F0F10"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        Padding="2"
                        SnapsToDevicePixels="True">
                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                </Border>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="ItemSurface" Property="Background" Value="#FF1F1F21"/>
            </Trigger>
            <Trigger Property="IsSubmenuOpen" Value="True">
                <Setter TargetName="ItemSurface" Property="Background" Value="#FF1F1F21"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="SubmenuItemTemplate" TargetType="{x:Type MenuItem}">
        <Border x:Name="ItemSurface"
                MinWidth="160"
                Background="Transparent"
                BorderBrush="Transparent"
                BorderThickness="0"
                Padding="10,5"
                SnapsToDevicePixels="True">
            <ContentPresenter ContentSource="Header"
                              RecognizesAccessKey="True"
                              HorizontalAlignment="Left"
                              VerticalAlignment="Center"
                              TextElement.Foreground="#FFBFC4C8"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="ItemSurface" Property="Background" Value="#FF1F1F21"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="ItemSurface" Property="Background" Value="#FF131314"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="TextElement.Foreground" Value="#FF7F7F81"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="#FFBFC4C8"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Style.Triggers>
            <Trigger Property="Role" Value="TopLevelHeader">
                <Setter Property="Template" Value="{StaticResource TopLevelMenuItemTemplate}"/>
            </Trigger>
            <Trigger Property="Role" Value="TopLevelItem">
                <Setter Property="Template" Value="{StaticResource SubmenuItemTemplate}"/>
            </Trigger>
            <Trigger Property="Role" Value="SubmenuItem">
                <Setter Property="Template" Value="{StaticResource SubmenuItemTemplate}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="StatusBarItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#FFE4F3F4"/>
    </Style>

    <Style TargetType="StatusBar">
        <Setter Property="Background" Value="#FF0B1218"/>
        <Setter Property="Foreground" Value="#FFE4F3F4"/>
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="#FFE4F3F4"/>
    </Style>

    <DrawingGroup x:Key="ChromeMinimizeIcon">
        <DrawingGroup.Children>
            <GeometryDrawing Brush="#FFE4F3F4" Geometry="F1 M 1,12 L 15,12 L 15,15 L 1,15 Z"/>
        </DrawingGroup.Children>
    </DrawingGroup>

    <DrawingGroup x:Key="ChromeCloseIcon">
        <DrawingGroup.Children>
            <GeometryDrawing Brush="#FFE4F3F4" Geometry="F1 M 8,10 L 12,10 L 16,14 L 20,10 L 24,10 L 18,16 L 24,22 L 20,22 L 16,18 L 12,22 L 8,22 L 14,16 Z"/>
        </DrawingGroup.Children>
    </DrawingGroup>

    <DrawingGroup x:Key="ChromeResizeIcon">
        <DrawingGroup.Children>
            <GeometryDrawing Brush="#FFE4F3F4" Geometry="F0 M 4,1 L 9,1 L 9,6 L 8,6 L 8,2 L 4,2 Z M 1,4 L 6,4 L 6,9 L 1,9 Z M 2,5 L 2,8 L 5,8 L 5,5 Z"/>
        </DrawingGroup.Children>
    </DrawingGroup>

    <Style x:Key="WindowChromeButton" TargetType="Button">
        <Setter Property="Width" Value="40"/>
        <Setter Property="Height" Value="31"/>
        <Setter Property="MinHeight" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#FFE4F3F4"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          RenderOptions.EdgeMode="Aliased"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#FF1F1F21"/>
                            <Setter Property="Foreground" Value="#FFE4F3F4"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#FF131314"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="WindowChromeCloseButton" TargetType="Button">
        <Setter Property="Width" Value="40"/>
        <Setter Property="Height" Value="31"/>
        <Setter Property="MinHeight" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="#FFE4F3F4"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          RenderOptions.EdgeMode="Aliased"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#FFE81123"/>
                            <Setter Property="Foreground" Value="#FFFFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#FFC50F1F"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background" Value="#FF0B1218"/>
        <Setter Property="Foreground" Value="#FFE4F3F4"/>
        <Setter Property="BorderBrush" Value="#FF1B1B1D"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="MinHeight" Value="26"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#FF1F1F21"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="#FF242428"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#FF131314"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="#FF0B1218"/>
        <Setter Property="Foreground" Value="#FFE4F3F4"/>
        <Setter Property="BorderBrush" Value="#FF1B1B1D"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="Padding" Value="8,3"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="Toggle"
                                      Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"
                                      Background="{TemplateBinding Background}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      BorderThickness="{TemplateBinding BorderThickness}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="Border"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="0">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#FF1F1F21"/>
                                            <Setter TargetName="Border" Property="BorderBrush" Value="#FF242428"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                            <Grid>
                                <ContentPresenter Margin="{TemplateBinding Padding}"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"/>
                                <Path HorizontalAlignment="Right"
                                      VerticalAlignment="Center"
                                      Margin="0,0,8,0"
                                      Fill="#FF4FD7D6"
                                      Data="M 0 0 L 8 0 L 4 5 Z"/>
                            </Grid>
                        </ToggleButton>
                        <Popup x:Name="PART_Popup"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               Placement="Bottom"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Fade">
                            <Border Background="#FF0B1218" BorderBrush="#FF1B1B1D" BorderThickness="1" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                <ScrollViewer MaxHeight="260">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="#FF0B1218"/>
        <Setter Property="Foreground" Value="#FFE4F3F4"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Border" Background="{TemplateBinding Background}">
                        <ContentPresenter Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#FF1F1F21"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#FF131314"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="#FFE4F3F4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border x:Name="Box"
                                Width="14"
                                Height="14"
                                Margin="0,0,6,0"
                                Background="#FF0B1218"
                                BorderBrush="#FF1B1B1D"
                                BorderThickness="1"
                                CornerRadius="0">
                            <Path x:Name="Check"
                                  Visibility="Collapsed"
                                  Stroke="#FF4FD7D6"
                                  StrokeThickness="2"
                                  Data="M 2 7 L 5 10 L 11 3"/>
                        </Border>
                        <ContentPresenter VerticalAlignment="Center"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="#FF242428"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Slider">
        <Setter Property="Foreground" Value="#FF4FD7D6"/>
        <Setter Property="Background" Value="#FF0B1218"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid Height="24" VerticalAlignment="Center">
                        <Border Height="4"
                                VerticalAlignment="Center"
                                Background="#FF071015"
                                BorderBrush="#FF1B1B1D"
                                BorderThickness="1"
                                CornerRadius="0"/>
                        <Track x:Name="PART_Track" VerticalAlignment="Center">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton IsTabStop="False" Opacity="0"/>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton IsTabStop="False" Opacity="0"/>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Width="10" Height="18">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Background="#FF4FD7D6"
                                                    BorderBrush="#FFA8F4F2"
                                                    BorderThickness="1"
                                                    CornerRadius="0"/>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SynthToggleCheckBox" TargetType="CheckBox">
        <Setter Property="Foreground" Value="#FF4FD7D6"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="Margin" Value="4,6"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <Style x:Key="SynthKeyWhite" TargetType="Button">
        <Setter Property="Margin" Value="1,0"/>
        <Setter Property="MinWidth" Value="28"/>
        <Setter Property="Background" Value="#FFD8E1DE"/>
        <Setter Property="Foreground" Value="#FF111820"/>
        <Setter Property="BorderBrush" Value="#FF667C7E"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="0,8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,4"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#FFBFCBC8"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SynthKeyBlack" TargetType="Button" BasedOn="{StaticResource SynthKeyWhite}">
        <Setter Property="Background" Value="#FF1E1E1E"/>
        <Setter Property="Foreground" Value="#FFCCCCCC"/>
        <Setter Property="MinWidth" Value="16"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Padding" Value="0,6"/>
        <Setter Property="Panel.ZIndex" Value="2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="#FF444444"
                            BorderThickness="1"
                            CornerRadius="0">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#FF333333"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
